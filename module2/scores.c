#include <stdio.h>

int main(void)
{
    int total = get_int("Total number of scores: ");
    int socres[total];
    float sum;
    for (int i = 0; i < total; i++)
    {
        scores[i] = get_int("%dº Score: ", i);
    }

    for (int i = 0; i < total; i++)
    {
        sum = scores[i];
    }

    float avg = sum / total

    printf("Average: %f\n",  / 3.);
}
