#include <cs50.h>
#include <stdio.h>
#include <math.h>
#include <ctype.h>

int main(int argc, int argv[])
{
    if (isdigit(argv)){
        string text = get_string("Text: ");
        string encipher = "";
    }
    else {
        printf("Usage: ./caesar key");
    }
}
