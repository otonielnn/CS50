#include <cs50.h>
#include <math.h>
#include <stdio.h>

int main(void)
{
    long card_number = get_int("Número: ");
    int sum = 0;
    int numbers_count = 0;

    while (card_number > 0)
    {
        int digit = 
    }
}
